---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app kromgo
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      strategy: rollback
      retries: 3
  values:
    controllers:
      kromgo:
        replicas: 1
        strategy: RollingUpdate
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: ghcr.io/home-operations/kromgo
              tag: 0.16.1@sha256:ad39dd32da600ea261112e7a098345d474d48c997790b0373eb651c66e490844
            env:
              KROMGO_SERVER_HOST: 0.0.0.0
              KROMGO_SERVER_PORT: &port 8080
              # kromgo is reachable from the public internet, so bound how long a
              # single request can hold a connection or pin a Prometheus query.
              # Upstream defaults are none, none, and 30s respectively.
              KROMGO_SERVER_READ_TIMEOUT: 5s
              KROMGO_SERVER_WRITE_TIMEOUT: 10s
              KROMGO_QUERY_TIMEOUT: 10s
              KROMGO_PROMETHEUS_URL:
                valueFrom:
                  secretKeyRef:
                    name: grafana-cloud-credentials
                    key: KROMGO_PROMETHEUS_URL
            probes:
              liveness: &probes
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /readyz
                    port: *port
                  initialDelaySeconds: 0
                  periodSeconds: 10
                  timeoutSeconds: 1
                  failureThreshold: 3
              readiness: *probes
            resources:
              requests:
                cpu: 10m
              limits:
                memory: 64Mi
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities:
                drop:
                  - ALL
    defaultPodOptions:
      # kromgo only queries Prometheus; it never calls the Kubernetes API, so it
      # has no need for a projected ServiceAccount token.
      automountServiceAccountToken: false
      dnsConfig:
        options:
          - name: ndots
            value: "1"
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: DoNotSchedule
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: *app
    route:
      app:
        annotations:
          glance/name: Kromgo
          glance/show: "false"
          glance/icon: https://cdn.jsdelivr.net/gh/homarr-labs/dashboard-icons/png/kubernetes.png
          glance/description: "Kubernetes metrics badge service."
          gatus.home-operations.com/endpoint: |
            path: /badges/talos_version
        hostnames:
          - "{{ .Release.Name }}.juno.moe"
        parentRefs:
          - name: external
            namespace: kube-system
            sectionName: https
        rules:
          - backendRefs:
              - identifier: app
                port: *port
    persistence:
      config:
        type: configMap
        name: kromgo-configmap
        globalMounts:
          - path: /config/config.yaml
            subPath: config.yaml
            readOnly: true
    service:
      app:
        ports:
          http:
            port: *port
