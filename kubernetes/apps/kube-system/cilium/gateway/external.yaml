---
# yaml-language-server: $schema=https://github.com/datreeio/CRDs-catalog/raw/refs/heads/main/gateway.networking.k8s.io/gateway_v1.json
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: external
  annotations:
    external-dns.alpha.kubernetes.io/target: "external.juno.moe"
    gatus.home-operations.com/endpoint: |
      client:
        dns-resolver: tcp://1.1.1.1:53
      alerts:
        - type: pushover
spec:
  gatewayClassName: cilium
  addresses:
    - type: IPAddress
      value: "192.168.69.104"
  infrastructure:
    annotations:
      external-dns.alpha.kubernetes.io/hostname: "external.juno.moe"
  listeners:
    - name: http
      protocol: HTTP
      port: 80
      hostname: "*.juno.moe"
      allowedRoutes:
        namespaces:
          from: Same
    - name: https
      protocol: HTTPS
      port: 443
      hostname: "*.juno.moe"
      allowedRoutes:
        namespaces:
          from: All
      tls:
        certificateRefs:
          - kind: Secret
            name: juno-moe-production-tls
