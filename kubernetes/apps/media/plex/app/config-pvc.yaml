---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: media
  name: plex-config-v1
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-block
  resources:
    requests:
      storage: 20Gi
