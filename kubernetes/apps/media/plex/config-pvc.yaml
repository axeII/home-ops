---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: media
  name: plex-config-v1
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 60Gi
