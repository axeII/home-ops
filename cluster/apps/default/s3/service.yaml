---
apiVersion: v1
kind: Service
metadata:
  name: "${SECRET_MINIO_SERVER}"
  namespace: default
  # labels:
    # app.kubernetes.io/name: *app
    # app.kubernetes.io/instance: *app
spec:
  type: ExternalName
  externalName: 192.168.69.101
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 80
    - name: https
      port: 443
      protocol: TCP
      targetPort: 443
